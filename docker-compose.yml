
services:
  workspace:
    build:
      context: .
      dockerfile: Dockerfile.workspace
    container_name: php-workspace
    volumes:
      - ./www:/var/www/html
    networks:
      - php-network
    stdin_open: true
    tty: true
    restart: unless-stopped

networks:
  php-network:
    driver: bridge

